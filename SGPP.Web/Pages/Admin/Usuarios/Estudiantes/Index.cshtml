@page
@model SGPP.Web.Pages.Admin.Usuarios.Estudiantes.IndexModel
@{
    ViewData["Title"] = "Gestión de Estudiantes";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Gestión de Estudiantes</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-page="/Admin/Dashboard">Dashboard</a></li>
        <li class="breadcrumb-item active">Estudiantes</li>
    </ol>

    <div class="card mb-4">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <span><i class="fas fa-user-graduate me-1"></i> Lista de Estudiantes</span>
            <a asp-page="./Create" class="btn btn-light btn-sm text-primary fw-bold">
                <i class="fas fa-plus"></i> Nuevo Estudiante
            </a>
        </div>
        <div class="card-body">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Nombre Completo</th>
                        <th>Email</th>
                        <th>Código</th>
                        <th>Carrera</th>
                        <th>Activo</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Estudiantes)
                    {
                        <tr>
                            <td>@item.NombreCompleto</td>
                            <td>@item.Email</td>
                            <td>@item.Codigo</td>
                            <td>@item.Carrera</td>
                            <td class="text-center">
                                @if(item.EsActivo) { <span class="badge bg-success">Si</span> } 
                                else { <span class="badge bg-danger">No</span> }
                            </td>
                            <td>
                                <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">
                                    <i class="fas fa-edit"></i> Editar
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
